public class MathMethods {
    public static void main(String []args){
        double dResult;

        //pow sqrt abs
        dResult=Math.pow(2,3);
        dResult=Math.abs(-500);
        dResult=Math.sqrt(144.0);

        //in calculations
        int r=14;
        double area=Math.PI*Math.pow(r, 2);

        
    }
}
